# Configurações do Banco de Dados para a VM (Homologação)
DB_PASSWORD=senha_vm_local_operabeauty
JWT_SECRET=super_secret_jwt_homologation_key_for_local_vm_testing_1234567890

# Configurações de Domínio e HTTPS (Caddy na VM sem HTTPS)
DOMAIN=localhost

# Webhooks para Monitoramento (Vazio para testes locais)
DISCORD_WEBHOOK_URL=
TELEGRAM_WEBHOOK_URL=

# Origens permitidas no CORS do Backend (Apenas para acesso local e da rede via VM)
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
